apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: letsencrypt-prod
spec:
  acme:
    email: adhirajkkinlekar@gmail.com
    server: https://acme-v02.api.letsencrypt.org/directory  # Use the production Let's Encrypt server
    privateKeySecretRef:
      name: letsencrypt-prod
    solvers:
    - dns01:
        cloudflare:
          apiTokenSecretRef:
            name: cloudflare-api-token-secret
            key: api-token
      selector:
        dnsZones:
         - veryown.in   # Only include the top-level domain 


# kubectl create secret generic cloudflare-api-token-secret \
# --from-literal=api-token=6yA6mmh-WAXyJ60zynm3AdQOej-V88Oozxenj9_h